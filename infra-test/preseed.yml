lookup_subnet: 172.25.0.3/16
lookup_interface: mcbr_bridge
systems:
  - name: micro-vm-2
    ovn_uplink_interface: mcbr_bridge
    storage:
      local:
        path: /dev/sdd
        wipe: True
      ceph:
        - path: /dev/sdc
          wipe: True
  - name: micro-vm-1
    ovn_uplink_interface: mcbr_bridge
    storage:
      local:
        path: /dev/sdc
        wipe: True
      ceph:
        - path: /dev/sdd
          wipe: True
  - name: micro-vm-0
    ovn_uplink_interface: mcbr_bridge
    storage:
      local:
        path: /dev/sdd
        wipe: True
      ceph:
        - path: /dev/sdc
          wipe: True
