lookup_subnet: 192.168.0.1/20
lookup_interface: mcbr_bridge
systems:
  - name: micro-vm-0
    ovn_uplink_interface: eth9
    storage:
      local:
        path: /dev/sdd
        wipe: True
      ceph:
        - path: /dev/sdc
          wipe: True
  - name: micro-vm-1
    ovn_uplink_interface: eth9
    storage:
      local:
        path: /dev/sdd
        wipe: True
      ceph:
        - path: /dev/sdc
          wipe: True
  - name: micro-vm-2
    ovn_uplink_interface: eth9
    storage:
      local:
        path: /dev/sdd
        wipe: True
      ceph:
        - path: /dev/sdc
          wipe: True
